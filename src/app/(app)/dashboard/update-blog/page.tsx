import { toast } from "sonner";

const UpdateBlog = () => {
    return (
        <div>
            <button onClick={() => toast("hello")}>sdf</button>
        </div>
    );
};

export default UpdateBlog;